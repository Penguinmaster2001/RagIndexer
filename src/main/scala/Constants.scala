package ragindexer

val CACHE_PATH = os.home / ".cache" / "rag-indexer" / "embeddings.json"

val OLLAMA = "http://localhost:11434"
val EMBED_MODEL = "nomic-embed-text-v2-moe"
val LLM_MODEL = "llama3.2"
